# TODO

- [x] Update `setup.py` so an empty `requirements.txt` (0 bytes) doesn’t break packaging; trim whitespace and ignore empty lines.
- [x] Re-run a packaging/install command (e.g., `pip install -e .`) to verify.


